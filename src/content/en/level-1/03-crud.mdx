import LessonLayout from '../../../components/layout/LessonLayout';

export const metadata = {
  title: "The Art of Creation and Destruction",
  level: "01",
  id: "1-3"
};

# Create and Destroy.

In Vim, you don't just type. You manipulate text with precise gestures.

<InfoBox type="tip" title="TODAY'S MANTRA">
Forget arrows. Start thinking in actions. Every key is a transformation command.
</InfoBox>

<Step number="1" title="Fundamental CRUD Commands">
Here's how to interact with text in **Normal Mode**:
</Step>

<ShortcutGrid 
  data={[
    { key: "i", label: "Insert", desc: "Insert before the cursor. (Enters Insert Mode)" },
    { key: "x", label: "Delete", desc: "Delete the character under the cursor." },
    { key: "u", label: "Undo", desc: "Undo the last action. Your best friend." },
    { key: "Ctrl + r", label: "Redo", desc: "Redo the undone action." },
  ]}
/>

<Step number="2" title="Practical Practice">
Move the cursor to the terminal on the right. Try deleting a word with <Kbd>x</Kbd> and then restoring it with <Kbd>u</Kbd>.
</Step>

<InfoBox type="warning" title="ATTENTION">
To return to **Normal Mode** after using <Kbd>i</Kbd>, always press <Kbd>ESC</Kbd>. Without it, you'll keep typing instead of commanding.
</InfoBox>

<Card title="Exercise">
  Try typing "Vim is King" in the terminal and then use <Kbd>u</Kbd> to travel back in time.
</Card>
