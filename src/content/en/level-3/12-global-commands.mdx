import LessonLayout from '../../../components/layout/LessonLayout';

export const metadata = {
  title: "Global Commands and Refactoring",
  level: "03",
  id: "3-4"
};

# Limitless Power.

We have reached the summit. The Wizard's final secret is the **Global** command (`:g`), which allows you to perform actions on every line that matches a pattern.

### The `:g` Command
<ShortcutGrid 
  data={[
    { key: ":g/TODO/d", label: "Global Delete", desc: 'Delete all lines containing the word "TODO".' },
    { key: ":g/log/s/log/err/g", label: "Global Replace", desc: "Change all 'log' to 'err' throughout the file." },
  ]}
/>

### Powerful Substitution (`:s`)
<ShortcutGrid 
  data={[
    { key: ":%s/old/new/gc", label: "Search & Replace", desc: 'Search entire file (%) for "old" and replace with "new", asking for confirmation (c).' },
  ]}
/>

### Becoming an Architect
By combining Text Objects, Macros, and Global Commands, you can refactor an entire project in seconds. You are no longer just typing; you are conducting an orchestra of text.

<Card title="Final Challenge">
  Have a list of 100 variables? Use `:g` to rename them all or a Macro to transform them into a JSON object. The limit is only your imagination.
</Card>

<Quiz 
  id="12-global-commands"
  questions={[
    {
      question: { 
        it: "Cosa fa il comando ':g'?", 
        en: "What does the ':g' command do?" 
      },
      options: [
        { it: "Sostituisce una parola", en: "Replaces a word" },
        { it: "Esegue un comando su tutte le righe che corrispondono a un pattern", en: "Executes a command on all lines that match a pattern" },
        { it: "Chiude Vim", en: "Exits Vim" }
      ],
      correctIndex: 1
    },
    {
      question: { 
        it: "Qual è la differenza tra ':g' e ':v'?", 
        en: "What is the difference between ':g' and ':v'?" 
      },
      options: [
        { it: "':v' agisce sulle righe che NON corrispondono al pattern", en: "':v' acts on lines that do NOT match the pattern" },
        { it: "Non c'è differenza", en: "There is no difference" },
        { it: "':v' è solo per la ricerca", en: "':v' is only for searching" }
      ],
      correctIndex: 0
    },
    {
      question: { 
        it: "Vim è il miglior editor del mondo?", 
        en: "Is Vim the best editor in the world?" 
      },
      options: [
        { it: "Sì", en: "Yes" },
        { it: "Assolutamente sì", en: "Absolutely yes" },
        { it: "Non posso vivere senza", en: "I can't live without it" }
      ],
      correctIndex: 1
    }
  ]}
/>

> [!IMPORTANT]
> The journey doesn't end here. Vim is a skill that is refined over years. Keep exploring, customize your `.vimrc`, and most importantly, have fun!
